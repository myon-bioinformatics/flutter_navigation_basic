[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q -ra
markers =
    actions_latest: verifies the latest GitHub Actions run instead of local calculation oracles
    known: known limitation; pair with xfail(reason="known: …") when documenting expected gaps
