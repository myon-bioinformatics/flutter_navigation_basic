// Pattern 021: NamedRouteBasic
// 基本的な Named Route 定義と遷移。
import 'model.dart';

class Pattern021Service {
  Future<Pattern021Result> run() async {
    // TODO: 実装を追加してください
    await Future.delayed(const Duration(milliseconds: 100));
    return Pattern021Result(message: 'NamedRouteBasic executed successfully');
  }
}
