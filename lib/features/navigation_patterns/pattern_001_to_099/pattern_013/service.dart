// Pattern 013: IndexedStack
// IndexedStack で状態保持しながら Tab 切り替え。
import 'model.dart';

class Pattern013Service {
  Future<Pattern013Result> run() async {
    // TODO: 実装を追加してください
    await Future.delayed(const Duration(milliseconds: 100));
    return Pattern013Result(message: 'IndexedStack executed successfully');
  }
}
