// Pattern 126: GlobalKey
// GlobalKey を使った Navigator 参照。
import 'model.dart';

class Pattern126Service {
  Future<Pattern126Result> run() async {
    // TODO: 実装を追加してください
    await Future.delayed(const Duration(milliseconds: 100));
    return Pattern126Result(message: 'GlobalKey executed successfully');
  }
}
