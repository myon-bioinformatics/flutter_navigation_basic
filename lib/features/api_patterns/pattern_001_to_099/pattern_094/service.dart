// Pattern 094: JsonNull
// null 値を含む JSON の安全なパース。
import 'model.dart';

class Pattern094Service {
  Future<Pattern094Result> run() async {
    // TODO: 実装を追加してください
    await Future.delayed(const Duration(milliseconds: 100));
    return Pattern094Result(message: 'JsonNull executed successfully');
  }
}
