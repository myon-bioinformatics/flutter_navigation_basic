// Pattern 093: JsonList
// JSON 配列の Dart List への変換。
import 'model.dart';

class Pattern093Service {
  Future<Pattern093Result> run() async {
    // TODO: 実装を追加してください
    await Future.delayed(const Duration(milliseconds: 100));
    return Pattern093Result(message: 'JsonList executed successfully');
  }
}
