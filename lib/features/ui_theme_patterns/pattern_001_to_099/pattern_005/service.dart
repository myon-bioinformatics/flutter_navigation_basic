// Pattern 005: IconButton
// アイコンボタンのバリエーション。
import 'model.dart';

class Pattern005Service {
  Future<Pattern005Result> run() async {
    // TODO: 実装を追加してください
    await Future.delayed(const Duration(milliseconds: 100));
    return Pattern005Result(message: 'IconButton executed successfully');
  }
}
