// Pattern 087: ButtonTheme
// ButtonStyle のグローバルカスタマイズ。
import 'model.dart';

class Pattern087Service {
  Future<Pattern087Result> run() async {
    // TODO: 実装を追加してください
    await Future.delayed(const Duration(milliseconds: 100));
    return Pattern087Result(message: 'ButtonTheme executed successfully');
  }
}
