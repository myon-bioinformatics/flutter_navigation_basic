// Pattern 129: LayoutBuilder
// LayoutBuilder で親サイズに応じた UI。
import 'model.dart';

class Pattern129Service {
  Future<Pattern129Result> run() async {
    // TODO: 実装を追加してください
    await Future.delayed(const Duration(milliseconds: 100));
    return Pattern129Result(message: 'LayoutBuilder executed successfully');
  }
}
