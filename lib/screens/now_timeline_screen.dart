import 'package:flutter/material.dart';

import '../features/now_timeline/presentation/now_timeline_page.dart';

class NowTimelineScreen extends StatelessWidget {
  const NowTimelineScreen({super.key});

  @override
  Widget build(BuildContext context) => const NowTimelinePage();
}
